<Page x:Class="Cups.Pages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:local="clr-namespace:Cups.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StatisticsPage">

    <Grid>
        <!--<lvc:CartesianChart Name="TestResultsChart">
            <lvc:CartesianChart.Series>
                <lvc:ColumnSeries Title="Test Scores" Values="{Binding TestResultsValues}" DataLabels="True">
                    <lvc:ColumnSeries.DataLabelsTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Foreground="Black" FontSize="16"/>
                        </DataTemplate>
                    </lvc:ColumnSeries.DataLabelsTemplate>
                </lvc:ColumnSeries>
            </lvc:CartesianChart.Series>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Tests" Labels="{Binding TestNames}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Points" MinValue="0" MaxValue="10"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>-->
    </Grid>
</Page>
